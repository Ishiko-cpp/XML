setting ISHIKO_CPP_XML_ROOT
{
    help = "Path to the Ishiko XML installation";
    default = '$(ISHIKO_CPP)/XML';
}

template IshikoXML
{
    includedirs += $(ISHIKO_CPP_XML_ROOT)/Include;

    if ($(toolset) == gnu)
    {
        libdirs += $(ISHIKO_CPP_XML_ROOT)/Lib;
    }

    if ($(toolset) != gnu)
    {
        libdirs += $(ISHIKO_CPP_XML_ROOT)/Lib/$(arch);
    }
}
